spring:
    datasource:
        username: root
        password: root
        url: jdbc:mysql://localhost:3307/workshop_db
    jpa:
        hibernate:
            ddl-auto: update
    jackson:
        default-property-inclusion: NON_NULL
    server:
        port: 8081
    logging:
        level:
            root: debug